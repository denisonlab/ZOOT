clear all


%% load data 

subs = {'pilot'};

for i=length(subs)
    SID = subs(i);
    folder = 'Users\jennymotzer\Documents\GitHub\ZOOT\experiment-files\data' subID 'beh'
    files = dir('*block*');
    for j=1:10
    